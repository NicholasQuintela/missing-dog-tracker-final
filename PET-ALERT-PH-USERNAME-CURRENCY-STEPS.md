# Pet Alert PH username and reward currency update

Run `pet-alert-ph-usernames-reward-currency.sql` once in Supabase SQL Editor before testing the website.

This update adds public usernames while keeping ownership tied to `auth.uid()`, and adds a selectable ISO currency to optional missing-pet rewards.
