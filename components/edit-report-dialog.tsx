"use client"
import {useEffect,useState} from "react"
import {Modal,Field,inputClass} from "@/components/modal"
import {Button} from "@/components/ui/button"
import {createClient} from "@/lib/supabase/client"
import type {MissingDog} from "@/lib/types"
export function EditReportDialog({open,onClose,dog,onUpdated}:{open:boolean;onClose:()=>void;dog:MissingDog|null;onUpdated:(d:MissingDog)=>void}){const [name,setName]=useState("");const[details,setDetails]=useState("");const[lastSeen,setLastSeen]=useState("");const[reward,setReward]=useState("");const[contact,setContact]=useState("");const[busy,setBusy]=useState(false);useEffect(()=>{if(dog){setName(dog.name);setDetails(dog.breed_details||"");setLastSeen(dog.last_seen||"");setReward(String(dog.reward||0));setContact(dog.contact_info)}},[dog,open]);if(!dog)return null;async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);const supabase=createClient();const{data,error}=await supabase.from("missing_dogs").update({name:name.trim(),breed_details:details.trim()||null,last_seen:lastSeen.trim()||null,reward:Number(reward)||0,contact_info:contact.trim()}).eq("id",dog.id).select().single();setBusy(false);if(error){alert(error.message);return}onUpdated(data as MissingDog);onClose()}return <Modal open={open} onClose={onClose} title={`Edit ${dog.name}'s report`}><form onSubmit={submit} className="flex flex-col gap-4"><Field label="Pet name"><input className={inputClass} value={name} onChange={e=>setName(e.target.value)} required/></Field><Field label="Description"><textarea className={inputClass+" h-auto py-2"} rows={3} value={details} onChange={e=>setDetails(e.target.value)}/></Field><Field label="Last seen"><input className={inputClass} value={lastSeen} onChange={e=>setLastSeen(e.target.value)}/></Field><Field label="Reward"><input className={inputClass} type="number" min="0" value={reward} onChange={e=>setReward(e.target.value)}/></Field><Field label="Contact"><input className={inputClass} value={contact} onChange={e=>setContact(e.target.value)} required/></Field><Button type="submit" disabled={busy}>{busy?"Saving…":"Save changes"}</Button></form></Modal>}
