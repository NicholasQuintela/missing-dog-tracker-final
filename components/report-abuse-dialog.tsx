"use client"
import {useState} from "react"
import {Modal,Field,inputClass} from "@/components/modal"
import {Button} from "@/components/ui/button"
import {createClient} from "@/lib/supabase/client"
type Props={open:boolean;onClose:()=>void;targetType:"missing_dog"|"sighting"|"user"|"message";targetId:string}
export function ReportAbuseDialog({open,onClose,targetType,targetId}:Props){const [reason,setReason]=useState("");const [details,setDetails]=useState("");const [busy,setBusy]=useState(false);const [error,setError]=useState<string|null>(null);async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setError(null);try{const supabase=createClient();const {data:{user}}=await supabase.auth.getUser();if(!user)throw new Error("Please log in to report abuse.");const {error}=await supabase.from("abuse_reports").insert({reporter_id:user.id,target_type:targetType,target_id:targetId,reason:reason.trim(),details:details.trim()||null});if(error)throw error;alert("Thank you. Your report was sent for review.");onClose();setReason("");setDetails("")}catch(e){setError(e instanceof Error?e.message:"Could not submit report.")}finally{setBusy(false)}}return <Modal open={open} onClose={onClose} title="Report abuse or suspicious content"><form onSubmit={submit} className="flex flex-col gap-4"><Field label="Reason" htmlFor="abuse-reason"><input id="abuse-reason" required minLength={5} maxLength={500} value={reason} onChange={e=>setReason(e.target.value)} className={inputClass} placeholder="Fake report, scam, harassment…"/></Field><Field label="Details (optional)" htmlFor="abuse-details"><textarea id="abuse-details" value={details} onChange={e=>setDetails(e.target.value)} className={inputClass+" h-auto py-2"} rows={4}/></Field>{error&&<p className="text-sm text-destructive">{error}</p>}<Button type="submit" disabled={busy}>{busy?"Sending…":"Submit abuse report"}</Button></form></Modal>}
