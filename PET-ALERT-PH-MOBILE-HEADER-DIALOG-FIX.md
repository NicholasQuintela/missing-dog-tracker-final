# Pet Alert PH Mobile Header and Dialog Fix

This frontend-only update fixes two phone layout issues:

- Report details modal close button remains inside the phone screen.
- Header report buttons use a second responsive row on small screens, so neither action is cut off.

No SQL or environment variable changes are required.
